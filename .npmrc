# Force using the exact versions specified in package.json
save-exact=true

# Use the legacy peer deps resolver to avoid conflicts
legacy-peer-deps=false

# Automatically install peer dependencies
auto-install-peers=true

